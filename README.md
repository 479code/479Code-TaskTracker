# 479Code Task Tracker

Simple task tracker for 479Code projects, backed by a Google Sheet.

- `index.html` — the tracker app (open this in a browser, or host it via GitHub Pages)
- `apps-script-backend.gs` — Apps Script code deployed on the Google Sheet that powers the GET/POST API

Data lives in the connected Google Sheet — open it any time to view or bulk-edit tasks directly.
